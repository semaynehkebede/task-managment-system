import React from 'react'
type archiveProjectProps = {
    selectedItem: any;
    onClose: (isOpened: boolean) => void;
}
const ArchiveTask = (props: archiveProjectProps) => {
    return (
        <div>

        </div>
    )
}

export default ArchiveTask
