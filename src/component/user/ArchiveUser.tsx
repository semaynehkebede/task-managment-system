import React from 'react'

type archiveUserProps = {
    selectedItem: any;
    onClose: (isOpened: boolean) => void;
}
const ArchiveUser = (props: archiveUserProps) => {
    return (
        <div>

        </div>
    )
}

export default ArchiveUser
