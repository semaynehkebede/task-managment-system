import React from 'react'
import { updateTaskProps } from '../task/TaskType'

const UpdateUser = (props: updateTaskProps) => {
    return (
        <div>

        </div>
    )
}

export default UpdateUser
